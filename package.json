{
  "name": "culture-center-finder",
  "version": "8.0.0",
  "private": true,
  "description": "문화센터 강좌 파인더 - 프로덕션 빌드 (esbuild, Babel-in-browser 제거)",
  "scripts": {
    "build": "node build.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.28.0"
  }
}
