# 변경 내역 (개선 작업)

## 세션6 (2026-07-07) — 모바일 플로팅 UI 정리 (사용자 신고: 화면 아이콘이 핵심 콘텐츠 가림)
### 문제
- 외부 자동봇이 v6~v14까지 매주 각자 독립적으로 `position:fixed` "퀵액션 레일"을 본문에 추가(문화 아이콘 스택). 각 패치가 개별 `@media(max-width:480px)` 가드를 넣었지만 실제 폰 CSS 뷰포트(480~540px대가 흔함)에서는 발동하지 않아, 좌측 세로 아이콘 스택(분류·강사·캘린더 등)·우측 세로 스택(레이더·포트폴리오·마일스톤 등)·하단 가로 바(트렌드·강사평가·품질배지 등)가 핵심 강좌 목록·필터를 그대로 가림. 추가로 v9~v13이 각자 띄우는 "새 버전 안내" 자체홍보 토스트(내부 버전명·기능목록)가 auto-dismiss 안 돼 여러 버전이 동시에 겹쳐 뜸.
### 조치
- `index.html`에 통합 CSS 규칙 3종 추가(각 vN_patch.js는 봇이 매주 재생성해 그 안에 고치면 소용없음 — index.html에 둬야 durable):
  1. 모바일(≤768px)에서 `[id$="-quick-actions"]`·`[id$="-qa-toggle"]` 속성선택자로 레일·토글버튼 전부 숨김 — 버전 무관, 향후 v15+도 자동 커버.
  2. 모달/온보딩 열릴 때 레일 숨김 가드를 v9 전용에서 전체 버전으로 일반화.
  3. `[id$="-toast"]`·`[class*="-toast"]`(v13은 id 대신 class 사용해 이 두 형태 다 필요)로 버전 안내 토스트를 화면 크기 무관 항상 숨김 — 내부 버전명을 사용자에게 노출하는 건 상용 서비스에 부적절.
- 헤드리스 브라우저로 실제 DOM의 `position:fixed/absolute` 요소 전수 조사 후 검증(모바일 뷰포트 412px 기준 정리 전/후 비교, v14 반영 후 재검증). 데스크탑(1440px)에서는 레일이 계속 보이도록 유지해 기존 기능·회귀 없음 확인.
### 검증
- 빌드 OK, E2E 7/7, 모바일 스크린샷상 좌우/하단 아이콘 완전 소거·핵심 표 전체 노출 확인, 데스크탑 스크린샷상 레일 정상 유지 확인.

## 세션5 (2026-07-04) — 마감강좌 필터링 신설 + 재크롤링
### 마감(수강신청 불가) 강좌 제거
- 이전까지는 각 소스 사이트에 이미 마감된 강좌도 그대로 노출되고 있었음(사용자 지적). status(idx10) 필드 — 각 크롤러가 사이트 자체 신호(버튼 텍스트·잔여석·API 상태코드)로 이미 계산해둔 등록상태 — 기준으로 마감 확정 강좌를 제거하는 `crawlers/filter_expired_courses.py` 신설.
- 개강일(idx13) 문자열은 소스마다 형식이 제각각이고, '상시' 강좌는 개강일이 수년 전으로 찍혀 있어도 지금 신청 가능해 날짜 비교로는 신뢰할 수 없음을 실데이터로 확인 → status 필드 기반 결정이 유일하게 안전한 방법.
- 제거 대상(신청 불가 확정): 마감·강의종료·접수마감·접수종료·접수불가·PAST·FULL. 보존 대상(신청 경로 존재): 접수중·신청가능·중간신청·대기접수·마감임박·대기신청·상시·모집중·모집예정·추가접수·대기등록·BEFORE·접수예정·현장문의.
- 결과: 81,327건 → **42,422건** (마감 38,905건, 47.8% 제거). `run_publish.py` 파이프라인에 편입(merge_all → **filter_expired_courses** → strip_instructor_names → 검증) — 다음 재크롤링부터 자동 적용.
- 검증 가드 재보정: 마감필터 도입으로 "총량" 기준선 자체가 바뀌어 기존 50,000 임계값이 상시 오탐되던 것을 30,000으로 조정 + 마감제거비율 80% 초과 시 경고(필터 오작동 감지용) 추가. 낡은 5/1 베이스라인 비교(이미 무의미했던 값)는 제거.
### 데이터 재크롤링
- 11개 소스 전체를 Haiku 에이전트 병렬(Workflow)로 재크롤링. 대부분 소스는 전일(7/3) 대비 강좌 카탈로그 자체는 거의 동일(학기 단위라 안정적) — 실제 변화는 신청상태(접수중↔마감) 위주라는 게 이번에 확인됨.
- 빌드 OK + E2E 7/7 + 개인정보(idx9) 0건 + 마감강좌 잔존 0건 확인 후 배포.

## 세션4 (2026-07-03) — 11소스 데이터 재크롤링 + 강사명 제거 파이프라인화
### 데이터 갱신
- 홈플러스·이마트·롯데마트·현대백화점·롯데백화점·신세계·서울평생학습·GSEEK·AK플라자·갤러리아·이랜드NC 11개 소스 전체 재크롤링(직전 2026-06-05 대비). 워크플로 에이전트로 병렬 실행(백그라운드 분리 실행 + 감시 방식, 대형 소스는 최대 십수 분 소요).
- 총 81,357건(직전 95,064건 대비 -14%). 소스별 자연 증감 확인(신세계·GSEEK는 학기 전환으로 감소, 롯데백화점·AK플라자·갤러리아·이랜드NC·서울평생학습은 증가) — 소스 0건·'기타'지역 25%초과 등 실패 가드 전부 통과.
### 개인정보 보호 (재발 방지)
- 재크롤링 시 강사 실명(idx9)이 원본에 그대로 포함돼 있어(2026-06-10 22d7aae 조치 이후 첫 재크롤링에서 재노출 위험 확인) 144,692건 재제거. 기존엔 재사용 스크립트 없이 1회성 수동 조치였던 것을 `crawlers/strip_instructor_names.py`로 신설하고 `run_publish.py` 파이프라인(fill_lotte_addr→merge_all→strip_instructor_names→검증)에 편입 — 다음 재크롤링부터 자동 적용되어 동일 문제 재발 차단.
- 빌드 OK + E2E 7/7 검증 후 배포.

## 세션3-R2 (2026-06-12) — 26에이전트 워크플로 적대검증 반영 (마무리)
독립 8차원 검증 + 적대적 재검증(26에이전트)이 적발한 실재 결함을 수정·재검증·배포.
### 접근성 (High)
- 키보드로 센터 상세 모달을 열 수 없던 문제 해소: 카드·표 센터명·통계 TOP10·표 즐겨찾기 트리거에 role="button"/tabIndex/aria-label/onKeyDown(Enter·Space) 추가. Playwright 키보드 테스트로 'Tab→Enter 모달 열림·ESC 닫힘' 실증(WCAG 2.1.1).
- add/edit·메모·삭제확인 모달에 role="dialog"/aria-modal/aria-label 추가(센터·비교 2개 + 3개 = 코어 5개 모달 일관).
### 빌드 무결성 (High)
- build.mjs가 v9_patch.js를 dist에 누락하던 문제 수정: 패치 파일을 하드코딩(v4~v8) 대신 정규식(/^v\d+_patch\.js$/)으로 자동 수집 → 봇이 v9·v10… 추가해도 dist 단독배포 무결. dist/v4~v9_patch.js 생성 확인.
### 보안 (Medium)
- v5 강좌 리뷰 후기 textarea raw 삽입(저장형 self-XSS) 차단: esc() 적용(v5_patch.js:1093).
### 문서
- README 'v4~v8'→'v4~v9', PROGRESS 모달 a11y 완료 반영.
### 잔여 (별도 항목, 본 범위 밖)
- 포커스 트랩·dist 배포 전환(GitHub Pages; build.mjs 준비완료)·27MB 데이터 SWR 캐싱.

## 세션3 (2026-06-12) — v9 통합 + 신청 동선·접근성 (각 빌드+E2E 7/7 회귀0, 라이브 배포 검증)
### 호환·통합
- 외부 자동봇 **v9.0**(학습경로맵·통계레이더·가격트렌드·포트폴리오·플래너·북마크·마일스톤·접근성패널·워드클라우드 등 + v9_patch.js 1,402줄)을 충돌 점검 후 통합. 봇 변경은 순수 추가형(index.html 메타+스크립트 1줄)이라 기존 개선 전부 보존 확인 → 깨끗이 rebase, 빌드+E2E 7/7 무손상.
### 전환·사용성 (신청 동선)
- 강좌 신청 경로 강화: 카드에 **'신청 →' 버튼**, 센터 상세 모달에 강좌별 **'신청' 컬럼**, 센터 전화번호 **클릭형 tel: 링크**, 목록 신청링크 확대(9→13px)+aria-label. 기존 9px 🔗 이모지 단일 진입점(막다른 길) 해소. (Fitts·Jakob — 명시적 텍스트 CTA·터치타겟 확대)
### 버그·UI 레이어
- v9 퀵액션 레일(#v9-quick-actions, z-index 940)이 코어 모달(z-index 100) 위로 떠 센터 모달 우측(신청 컬럼)을 가리던 문제 수정: `body:has(.modal-overlay) #v9-quick-actions{display:none}` — 모달 열림 시 자동 숨김. index.html CSS라 v9_patch 주간 재생성에도 유지(durable).
### 접근성 (WCAG)
- 센터·비교 모달에 `role="dialog"`/`aria-modal="true"`/aria-label 추가 — 보조기기 모달 인식·Lighthouse 접근성.
### 검증·배포
- 각 변경 빌드 0에러·E2E 7/7·콘솔에러 0. master 배포 후 라이브(bsy522-dot.github.io/culture-center-finder)에서 신청버튼·전화링크·레일숨김·v9 서빙 직접 확인. 커밋 a3e0bf3·cc32334·d9c192f.

## improve/20260610 — 상용화 개선 세션2 (각 변경 빌드+E2E 7/7 회귀0 검증)
### 데이터·개인정보·저작권
- 미사용 정크 파일 31개 제거(배포 120MB→54MB): 백업 데이터의 강사 실명 65,987건, 경쟁사 원본 회원포털 HTML 7개, 타사 스크린샷 등 공개 노출 차단. `.gitignore` 보강으로 재유입 방지. (앱은 all.json·center_coords.json만 사용 → 회귀0)
### 접근성 (WCAG)
- 핵심 입력 4곳 aria-label 추가(검색·출발지·이동시간·출생년도) — 스크린리더 대응.
- 색상대비 WCAG AA 통과: muted/faint/필터텍스트 토큰 — 다크 3.13→7.26:1·1.78→4.9:1, 라이트 2.45→6.0:1.
### 성능
- 데이터 fetch 캐시버스터(`?t=Date.now()`) 제거 — 매 방문 27MB 강제 재다운로드 → 변경 시에만 다운로드(304). network-first+ETag로 신선도 유지.
### 기능·법적
- v7 가짜 강사평점 제거 — 실제 상호(홈플러스·롯데마트·갤러리아 등)에 허구 강사 12명·가짜 별점을 결부(명예훼손·기만 소지)하던 것 삭제, "준비 중" 안내로 대체.
### 검증
- 프로덕션 빌드 0에러, node --check 전 JS 0오류, npm audit 0취약점, E2E 핵심3시나리오 7/7, 코드리뷰 교차검증.

## improve/20260609 — 상용화 개선 (진행 중)
### 보안
- 저장형 XSS 19곳 차단: esc() 헬퍼 추가(v4~v8) + 사용자입력(노트·위시리스트·커뮤니티·일기·알림·플래너·닉네임) 이스케이프.
- 하드코딩 비밀키 스캔: 0건 확인.

### 데이터·개인정보 (우회전략)
- 강사 실명 180,152건 제거(공개 배포본). 학부모 선택 필수정보(강좌명·가격·요일·시간·시작일·기간·카테고리)는 전부 유지.
- 내부 전체데이터는 .full.bak로 보존(gitignore, 공개 차단).

### 빌드·테스트·문서
- 프로덕션 빌드 도입(esbuild, JSX 사전컴파일 → Babel-인-브라우저 제거): dist/ 생성.
- 핵심 3시나리오 E2E 테스트(Playwright): tests/e2e_munsen.py.
- 배포·롤백 가이드(DEPLOY.md), 진행상황(PROGRESS.md), 개선계획서(개선계획서.html).

### 다음 (계획)
- 성능(29MB 데이터 분할 로드), 첫화면 정리(런처 통합), 접근성(WCAG AA), 끊긴 기능 복구, 시간표 기능.
