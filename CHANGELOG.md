# 변경 내역 (개선 작업)

## 세션11 (2026-07-13) — 홈플러스 표기 중단 + 연령필터 정밀화 (긴급 대응 2건)
### 홈플러스 배포 제외
- 홈플러스 회생절차 폐지(7/3)·전국 67개점 임시휴업(7/13, 경향 202607131031001)으로 강좌 정보 신뢰 불가 — 사용자 지시로 표기 중단. `merge_all.py` `EXCLUDED_SOURCES` 스위치 신설(크롤 파일은 계속 수집·보존, 병합에서만 제외 — 영업 정상화 시 한 줄 제거로 복구). run_publish 가드가 의도적 제외를 급감으로 오인하지 않도록 meta.excluded_sources 연동. 푸터 소스 목록·README 갱신. 15,871→**14,365건**.
### 연령필터 정밀화 (사용자 신고: 2020년생에 만 4-5세 강좌 노출)
- 대상 필드가 '유아'처럼 광범위해도 강좌명 속 정밀 연령("(만 4-5세)"·"(16-23개월)"·"7세 이상")을 1순위로 파싱 + `hi+1` 여유 비교 제거(엄격). 개월 단위 영아 강좌가 6세 필터에 노출되던 문제도 함께 해소. 트레이드오프: 생일 안 지난 아이의 경계 연령 강좌가 일부 안 보일 수 있음(오노출보다 안전 우선).

## 세션10 (2026-07-13) — 사용감 극대화 8종 (배포 전 완성도 끌어올리기)
### 핵심 동선
- **강좌 상세 시트**: 카드/강좌명 탭 → 강좌 상세(모바일 바텀시트·데스크탑 모달) — 큰 "신청하러 가기" 버튼, 상태·개강 D-day·대상 칩, 일정/수강료/주소(지도)/전화, 저장·공유·메모·센터 버튼. 기존엔 카드를 눌러도 센터 정보만 떴음.
- **공유 딥링크**: `#c=행키` URL — 공유(navigator.share/클립보드)로 받은 링크를 열면 해당 강좌 상세가 바로 열림. 초기 마운트 시 해시가 데이터 로드 전에 소거되던 경합 버그 수정(실측).
- **기본 정렬 = 개강 임박순** + 카드/표 D-day 뱃지(3일 이내 강조). 잔존 강좌가 전부 개강 전이므로 가장 유용한 순서.
- **검색 업그레이드**: 초성 검색(ㅇㄱ→요가) + 자동완성(카테고리·센터, 상위 6) + 행별 검색 인덱스 캐시.
### 성능·품질
- **렌더 예산 전역화**: 그룹별 slice(0,100)가 그룹 수만큼 곱해져 모바일 카드 DOM이 3,900+개였던 문제 → 전역 예산으로 초기 100개(스크롤 시 증분). 저가폰 스크롤 개선.
- **Lighthouse**: 접근성 89→96, 모범사례 96→100, SEO 100 — 파비콘 404, 라벨 없는 select, 푸터 h4 헤딩 건너뜀, 카드 aria-label 불일치, 모바일 탭타깃(체크박스 22px 등) 수정 + unpkg preconnect. 푸터의 낡은 "84,000+" 문구도 현행화.
### 신기능
- **센터 지도**(🗺️ 버튼): 현재 필터 기준 센터를 지도에 마커(강좌수 비례 크기)로 표시, 팝업에서 "이 센터 강좌 보기" → 목록 필터. Leaflet 1.9.4 지연 로드(첫 열림 시), 출발지 설정 시 내 위치 표시. 좌표 98% 확보(세션7)로 가능해진 기능.
- **첫 실행 2문답**: 온보딩 첫 화면에서 출생년도+동네만 입력하면 즉시 맞춤 목록(연령 필터+이동시간 계산). 건너뛰기 가능. ※이 과정에서 applyAddr의 스테일 클로저(비동기 완료 시 옛 settings로 덮어써 방금 저장한 출생년도가 지워짐) 버그 발견·수정.
### 검증
- build OK ×4, E2E 7/7 ×3, 헤드리스 실증(시트·딥링크 재열림·초성검색·자동완성·D-day 오름차순·카드 100개·지도 마커 310+타일·온보딩 저장 birthYear/address), Lighthouse 재감사로 수치 확인.

## 세션9 (2026-07-12) — 잔여 백로그 일괄 소진 (워크플로 3에이전트 병렬 + 앱 코어 4건)
### 앱 코어 (index.html)
- **데이터 기준일 동적 표시**: 헤더 강좌수 옆에 meta.json의 updated_at을 "| 데이터 YYYY-MM-DD"로 표시(하드코딩 제거).
- **모바일 카드뷰 기본**: <768px 첫 화면을 카드뷰로(13컬럼 표의 가로스크롤·미세 터치 문제 해소, 토글로 표 전환 가능). 데스크탑은 표 유지.
- **터치타깃 확대(모바일)**: 별표·비교체크·필터칩·하단내비 8~12px → 32~44px(권장 36px+).
- **모달 포커스 트랩(a11y)**: 모달 열림 시 포커스 이동+Tab 순환 가둠+닫힘 시 복귀(WCAG 2.4.3).
- ※신청·전화 버튼(카드/센터모달)은 기왕 구현돼 있음을 확인 — 백로그 항목 종결.
### 데이터 품질
- **'기타' 종목 회수**: utils.py classify()에 회수 규칙 + merge_all.py에 기존 '기타'행 재분류 패스(재크롤 불요 소급). 기타 31.0%→**19.3%**(-1,898건: 뷰티·섬유/뜨개·재테크·요리·아로마/캔들·요가/필라테스·놀이·영재교육 등 기존 카테고리로). 오탐 가드(쉬림프≠림프, 커피박키링≠요리, 미니어처초밥≠요리, ITQ한글≠어학 등) + 표본 36건 육안검증.
- **소스 급감 가드**: run_publish가 직전 meta 스냅샷(meta_prev.json, gitignore) 대비 소스별 70%+ 감소 시 경고→배포 차단. 학기전환 자연감소(~45%)는 통과.
### SEO
- crawlers/gen_seo_pages.py 신설(파이프라인 6단계로 편입, 멱등): 지역 랜딩 18페이지(app/seo/) + sitemap.xml(19 URL) + robots.txt. build.mjs가 dist에 포함.
### 운영 정리
- app_deploy/(143MB, 방문자 캐시를 파괴하는 옛 sw.js 보유 클론 — 6/9 감사 지적) → D:\AI\_deleted_files\munsen_app_deploy_20260712\ 격리.
- ※GitHub Actions 크롤 자동화(백로그)는 의도적 미채택: 서울포털이 로컬 IP도 403 차단하는 상황에서 데이터센터 IP는 더 막힘 — 로컬 update_all.py(hermes)가 정답.
### 검증
- build OK, E2E 7/7(×2), 모바일 카드뷰/기준일/포커스트랩/터치타깃 헤드리스 실증, 파이프라인 검증 통과(15,871건 — 날짜 경과분 자연 감소 포함, 기타지역 1.5%).

## 세션8 (2026-07-10 저녁) — 시작일 경과 강좌 제거 + 11소스 당일 재크롤링 (사용자 지시: "시작일이 지났는데도 있으면 안 된다")
### 시작일 경과 필터 신설 (filter_expired_courses.py 확장)
- 기존 status 기준 마감 필터는 "지금도 신청 가능(중간 합류 포함)"이면 유지했기 때문에, 목록의 56%(23,737건)가 개강일이 지난 강좌였음 — 사용자 지시로 **개강일이 오늘 이전이면 상태 무관 제거**로 정책 변경.
- 개강일(idx13)을 YYYY.MM.DD로 정규화 저장(롯데백화점 'M/D' 연도 없는 형식은 올해로 복원 — 백로그의 연도복원 항목도 함께 해소). 날짜 없음/파싱 불가는 판단 불가라 유지. 단위테스트 9케이스 통과.
- 검증 가드 재보정: 총량 하한 30,000→8,000, 제거비율 상한 80%→90%(마감 ~48%+시작일경과 추가로 총 75~80% 제거가 새 정상).
### 데이터 재크롤링 (2026-07-10)
- 11소스 병렬 재크롤링. 10소스 당일 신선본, **서울평생학습포털은 전 채널 HTTP 403(서버측 차단, 2회 시도)** → 7/4 수집본 유지(다음 회차 재시도 필요).
- 결과: 원천 76,243 → 마감 39,880 + 시작일경과 19,959 제거 = **16,404건**(신청 가능 + 개강 전만). 강사명 재유입 31,096건 자동 제거(파이프라인), 신규 센터 좌표 증분 지오코딩(311/317, 98%), '기타' 지역 1.4%, 검증 통과.
- 크롤러 기동 시 주의: 기본 python(hermes venv)엔 bs4 없음 — 홈플러스·현대는 Python311로 기동해야 함(이번에 1회 재기동 발생).
### UI 정리
- "진행중" 시기 칩 제거(시작일 경과 강좌가 데이터에서 사라져 결과 0인 죽은 버튼이 되므로). 표의 " 진행중" 라벨 코드는 방어용으로 유지(크롤 간격 사이 개강한 강좌 대비).

## 세션7 (2026-07-10) — 전면 개선 6종 (지오코딩·안정키·에러방어·분할로딩·dist전환·가짜기능 정리)
### ① 센터 좌표 전수 지오코딩 — 거리·이동시간 기능 복구
- 좌표 커버가 106/479센터(강좌 기준 22%)뿐이라 "가까운 순"·"이동시간 필터"에서 나머지 센터 강좌가 통째로 제외(999분 처리)되던 문제. `crawlers/geocode_centers.py` 신설(Nominatim 1req/1.1s, 한국 bbox 검증, 주소→축약주소→센터명→지명 휴리스틱 4단 질의, 20건마다 중간저장) → **473/479센터, 강좌 기준 98%+ 커버**. `run_publish.py` 파이프라인에 편입(신규 센터만 증분 처리)해 재크롤링 후에도 자동 유지.
- 잔여 6곳(이마트 고잔·검단·흥덕·포항이동·스타필드시티명지, 신세계 천안아산)은 주소 데이터가 없고 지점명 지명이 동명이지역 오매칭 위험이 있어 의도적으로 보류(종전과 동일하게 거리 미제공 처리).
### ② 즐겨찾기·메모 안정키 전환 — 데이터 갱신 때마다 별표가 엉뚱한 강좌로 옮겨가던 문제 근절
- fav/memo가 all.json 배열 인덱스로 localStorage 저장돼, 재크롤링 배포마다 행 순서가 바뀌면 사용자 별표·메모가 조용히 다른 강좌로 오염되던 구조. 행 내용 기반 합성키(센터|강좌명|시간 — merge_all dedup키와 동일 구성)로 전환(`rowKey()`), 저장키 cc-fav→cc-fav2, cc-memo→cc-memo2(구 키는 정리 — 구 데이터는 이미 어긋난 인덱스라 이관 불가). 행 삭제 시 인덱스 재계산 로직도 불필요해져 제거.
### ③ 검은화면 방지 2중 방어막
- (a) 순수 JS 워치독(Babel/React가 죽어도 동작): CDN 실패·컴파일 실패·초기 크래시로 #root가 비면 12초 내 안내+"다시 불러오기" 버튼 표시. React CDN 차단 주입 테스트로 발동 실증. (b) React ErrorBoundary: 렌더링 중 크래시 시 앱 전체가 빈 화면으로 죽지 않고 오류 안내+새로고침 버튼 표시.
### ④ 로딩 속도 — 지역 분할 로딩 + Babel 제거(dist 배포 전환)
- (a) 접속 즉시 전국 14MB(all.json)를 통짜로 받던 것을 regions/*.json 18개 병렬 분할 로딩으로 전환: 첫 지역 도착 즉시 화면 표시, 나머지는 도착 순 병합(진행률 문구 표시), 최종 순서는 index.json 순서로 고정. 실패 시 all.json 통짜 로딩 폴백. 검증: 지역요청 19건/all.json 0건, 42,422건 전량 로드.
- (b) GitHub Pages를 legacy(master 루트=Babel-in-browser)에서 **GitHub Actions 빌드 배포(dist/)**로 전환: `.github/workflows/deploy.yml` 신설 — master 푸시(봇 [AUTO] 포함)마다 esbuild 사전컴파일 후 배포. @babel/standalone 런타임 ~2.4MB 다운로드+폰 CPU 즉석 컴파일 제거(app.js 82KB), 2026-06-20형 Babel CDN 전면장애 취약점도 원천 제거. 빌드 실패 시 직전 성공 배포 유지(사이트 다운 없음). build.mjs에 privacy.html 복사 추가(처리방침 URL 유지 — 심사 하드게이트).
### ⑤ 봇 가짜기능 정리 — v8~v15 로드 차단 (12개 패치 전수감사)
- 전수감사 결과(파일별 하드코딩 배열 변수·라인 근거 확보): v8~v15는 실데이터 접근 경로가 전무하고 가짜 커뮤니티 멤버·가짜 리뷰·가짜 강사 포트폴리오·Math.random 수료율/리더보드/시설평점 등 **조작 정보를 실데이터처럼 노출**(상용 치명). v4~v7만 실데이터(fetch훅 __v4Data·React fiber)/유저 로컬데이터 기반. 패치 간 의존성은 v5→v4 단 하나 → index.html에서 v8~v15 script 태그 8개 제거(파일은 보존). 데스크탑 v4·v5·v7 레일 정상 유지 확인.
- ※봇이 추가할 v16+ 태그는 다시 붙으므로 주기 점검 시 동일 기준(실데이터 여부)으로 심사 필요.
### ⑥ "지남" 표기·죽은 시기필터 수정
- 마감강좌 필터링(세션5) 이후 데이터엔 신청 가능 강좌만 남으므로, 개강일 지난 강좌의 " 지남" 표기(마감 오인 유발)를 " 진행중"으로 교체.
- "시기:" 필터 칩이 로직이 인식 못 하는 값(접수중/대기접수/…)으로 배선돼 **사실상 무동작이던 버그** 수정: 칩을 설계 값(신규/모집중·신규강좌·모집중·진행중)으로 복원하고, 모집중 판정 상태 목록을 실데이터 status 22종 기준으로 확장. 클릭 시 실제 필터링 동작 확인(4,348→3,928행).
### 검증
- npm build OK ×3, E2E 7/7(루트·dist 각각), 심층검증(지역 분할로딩 실사용·fav2 영속성·새로고침 후 별표 유지·시기필터 실동작·"지남" 소거), React CDN 차단 크래시 주입 → 워치독 폴백 발동 확인, 모바일(412px) 플로팅 요소 온보딩·하단내비만 잔존, 데스크탑(1440px) 회귀 없음, page error 0.

## 세션6 (2026-07-07) — 모바일 플로팅 UI 정리 (사용자 신고: 화면 아이콘이 핵심 콘텐츠 가림)
### 문제
- 외부 자동봇이 v6~v14까지 매주 각자 독립적으로 `position:fixed` "퀵액션 레일"을 본문에 추가(문화 아이콘 스택). 각 패치가 개별 `@media(max-width:480px)` 가드를 넣었지만 실제 폰 CSS 뷰포트(480~540px대가 흔함)에서는 발동하지 않아, 좌측 세로 아이콘 스택(분류·강사·캘린더 등)·우측 세로 스택(레이더·포트폴리오·마일스톤 등)·하단 가로 바(트렌드·강사평가·품질배지 등)가 핵심 강좌 목록·필터를 그대로 가림. 추가로 v9~v13이 각자 띄우는 "새 버전 안내" 자체홍보 토스트(내부 버전명·기능목록)가 auto-dismiss 안 돼 여러 버전이 동시에 겹쳐 뜸.
### 조치
- `index.html`에 통합 CSS 규칙 3종 추가(각 vN_patch.js는 봇이 매주 재생성해 그 안에 고치면 소용없음 — index.html에 둬야 durable):
  1. 모바일(≤768px)에서 `[id$="-quick-actions"]`·`[id$="-qa-toggle"]` 속성선택자로 레일·토글버튼 전부 숨김 — 버전 무관, 향후 v15+도 자동 커버.
  2. 모달/온보딩 열릴 때 레일 숨김 가드를 v9 전용에서 전체 버전으로 일반화.
  3. `[id$="-toast"]`·`[class*="-toast"]`(v13은 id 대신 class 사용해 이 두 형태 다 필요)로 버전 안내 토스트를 화면 크기 무관 항상 숨김 — 내부 버전명을 사용자에게 노출하는 건 상용 서비스에 부적절.
- 헤드리스 브라우저로 실제 DOM의 `position:fixed/absolute` 요소 전수 조사 후 검증(모바일 뷰포트 412px 기준 정리 전/후 비교, v14 반영 후 재검증). 데스크탑(1440px)에서는 레일이 계속 보이도록 유지해 기존 기능·회귀 없음 확인.
### 검증
- 빌드 OK, E2E 7/7, 모바일 스크린샷상 좌우/하단 아이콘 완전 소거·핵심 표 전체 노출 확인, 데스크탑 스크린샷상 레일 정상 유지 확인.

## 세션5 (2026-07-04) — 마감강좌 필터링 신설 + 재크롤링
### 마감(수강신청 불가) 강좌 제거
- 이전까지는 각 소스 사이트에 이미 마감된 강좌도 그대로 노출되고 있었음(사용자 지적). status(idx10) 필드 — 각 크롤러가 사이트 자체 신호(버튼 텍스트·잔여석·API 상태코드)로 이미 계산해둔 등록상태 — 기준으로 마감 확정 강좌를 제거하는 `crawlers/filter_expired_courses.py` 신설.
- 개강일(idx13) 문자열은 소스마다 형식이 제각각이고, '상시' 강좌는 개강일이 수년 전으로 찍혀 있어도 지금 신청 가능해 날짜 비교로는 신뢰할 수 없음을 실데이터로 확인 → status 필드 기반 결정이 유일하게 안전한 방법.
- 제거 대상(신청 불가 확정): 마감·강의종료·접수마감·접수종료·접수불가·PAST·FULL. 보존 대상(신청 경로 존재): 접수중·신청가능·중간신청·대기접수·마감임박·대기신청·상시·모집중·모집예정·추가접수·대기등록·BEFORE·접수예정·현장문의.
- 결과: 81,327건 → **42,422건** (마감 38,905건, 47.8% 제거). `run_publish.py` 파이프라인에 편입(merge_all → **filter_expired_courses** → strip_instructor_names → 검증) — 다음 재크롤링부터 자동 적용.
- 검증 가드 재보정: 마감필터 도입으로 "총량" 기준선 자체가 바뀌어 기존 50,000 임계값이 상시 오탐되던 것을 30,000으로 조정 + 마감제거비율 80% 초과 시 경고(필터 오작동 감지용) 추가. 낡은 5/1 베이스라인 비교(이미 무의미했던 값)는 제거.
### 데이터 재크롤링
- 11개 소스 전체를 Haiku 에이전트 병렬(Workflow)로 재크롤링. 대부분 소스는 전일(7/3) 대비 강좌 카탈로그 자체는 거의 동일(학기 단위라 안정적) — 실제 변화는 신청상태(접수중↔마감) 위주라는 게 이번에 확인됨.
- 빌드 OK + E2E 7/7 + 개인정보(idx9) 0건 + 마감강좌 잔존 0건 확인 후 배포.

## 세션4 (2026-07-03) — 11소스 데이터 재크롤링 + 강사명 제거 파이프라인화
### 데이터 갱신
- 홈플러스·이마트·롯데마트·현대백화점·롯데백화점·신세계·서울평생학습·GSEEK·AK플라자·갤러리아·이랜드NC 11개 소스 전체 재크롤링(직전 2026-06-05 대비). 워크플로 에이전트로 병렬 실행(백그라운드 분리 실행 + 감시 방식, 대형 소스는 최대 십수 분 소요).
- 총 81,357건(직전 95,064건 대비 -14%). 소스별 자연 증감 확인(신세계·GSEEK는 학기 전환으로 감소, 롯데백화점·AK플라자·갤러리아·이랜드NC·서울평생학습은 증가) — 소스 0건·'기타'지역 25%초과 등 실패 가드 전부 통과.
### 개인정보 보호 (재발 방지)
- 재크롤링 시 강사 실명(idx9)이 원본에 그대로 포함돼 있어(2026-06-10 22d7aae 조치 이후 첫 재크롤링에서 재노출 위험 확인) 144,692건 재제거. 기존엔 재사용 스크립트 없이 1회성 수동 조치였던 것을 `crawlers/strip_instructor_names.py`로 신설하고 `run_publish.py` 파이프라인(fill_lotte_addr→merge_all→strip_instructor_names→검증)에 편입 — 다음 재크롤링부터 자동 적용되어 동일 문제 재발 차단.
- 빌드 OK + E2E 7/7 검증 후 배포.

## 세션3-R2 (2026-06-12) — 26에이전트 워크플로 적대검증 반영 (마무리)
독립 8차원 검증 + 적대적 재검증(26에이전트)이 적발한 실재 결함을 수정·재검증·배포.
### 접근성 (High)
- 키보드로 센터 상세 모달을 열 수 없던 문제 해소: 카드·표 센터명·통계 TOP10·표 즐겨찾기 트리거에 role="button"/tabIndex/aria-label/onKeyDown(Enter·Space) 추가. Playwright 키보드 테스트로 'Tab→Enter 모달 열림·ESC 닫힘' 실증(WCAG 2.1.1).
- add/edit·메모·삭제확인 모달에 role="dialog"/aria-modal/aria-label 추가(센터·비교 2개 + 3개 = 코어 5개 모달 일관).
### 빌드 무결성 (High)
- build.mjs가 v9_patch.js를 dist에 누락하던 문제 수정: 패치 파일을 하드코딩(v4~v8) 대신 정규식(/^v\d+_patch\.js$/)으로 자동 수집 → 봇이 v9·v10… 추가해도 dist 단독배포 무결. dist/v4~v9_patch.js 생성 확인.
### 보안 (Medium)
- v5 강좌 리뷰 후기 textarea raw 삽입(저장형 self-XSS) 차단: esc() 적용(v5_patch.js:1093).
### 문서
- README 'v4~v8'→'v4~v9', PROGRESS 모달 a11y 완료 반영.
### 잔여 (별도 항목, 본 범위 밖)
- 포커스 트랩·dist 배포 전환(GitHub Pages; build.mjs 준비완료)·27MB 데이터 SWR 캐싱.

## 세션3 (2026-06-12) — v9 통합 + 신청 동선·접근성 (각 빌드+E2E 7/7 회귀0, 라이브 배포 검증)
### 호환·통합
- 외부 자동봇 **v9.0**(학습경로맵·통계레이더·가격트렌드·포트폴리오·플래너·북마크·마일스톤·접근성패널·워드클라우드 등 + v9_patch.js 1,402줄)을 충돌 점검 후 통합. 봇 변경은 순수 추가형(index.html 메타+스크립트 1줄)이라 기존 개선 전부 보존 확인 → 깨끗이 rebase, 빌드+E2E 7/7 무손상.
### 전환·사용성 (신청 동선)
- 강좌 신청 경로 강화: 카드에 **'신청 →' 버튼**, 센터 상세 모달에 강좌별 **'신청' 컬럼**, 센터 전화번호 **클릭형 tel: 링크**, 목록 신청링크 확대(9→13px)+aria-label. 기존 9px 🔗 이모지 단일 진입점(막다른 길) 해소. (Fitts·Jakob — 명시적 텍스트 CTA·터치타겟 확대)
### 버그·UI 레이어
- v9 퀵액션 레일(#v9-quick-actions, z-index 940)이 코어 모달(z-index 100) 위로 떠 센터 모달 우측(신청 컬럼)을 가리던 문제 수정: `body:has(.modal-overlay) #v9-quick-actions{display:none}` — 모달 열림 시 자동 숨김. index.html CSS라 v9_patch 주간 재생성에도 유지(durable).
### 접근성 (WCAG)
- 센터·비교 모달에 `role="dialog"`/`aria-modal="true"`/aria-label 추가 — 보조기기 모달 인식·Lighthouse 접근성.
### 검증·배포
- 각 변경 빌드 0에러·E2E 7/7·콘솔에러 0. master 배포 후 라이브(bsy522-dot.github.io/culture-center-finder)에서 신청버튼·전화링크·레일숨김·v9 서빙 직접 확인. 커밋 a3e0bf3·cc32334·d9c192f.

## improve/20260610 — 상용화 개선 세션2 (각 변경 빌드+E2E 7/7 회귀0 검증)
### 데이터·개인정보·저작권
- 미사용 정크 파일 31개 제거(배포 120MB→54MB): 백업 데이터의 강사 실명 65,987건, 경쟁사 원본 회원포털 HTML 7개, 타사 스크린샷 등 공개 노출 차단. `.gitignore` 보강으로 재유입 방지. (앱은 all.json·center_coords.json만 사용 → 회귀0)
### 접근성 (WCAG)
- 핵심 입력 4곳 aria-label 추가(검색·출발지·이동시간·출생년도) — 스크린리더 대응.
- 색상대비 WCAG AA 통과: muted/faint/필터텍스트 토큰 — 다크 3.13→7.26:1·1.78→4.9:1, 라이트 2.45→6.0:1.
### 성능
- 데이터 fetch 캐시버스터(`?t=Date.now()`) 제거 — 매 방문 27MB 강제 재다운로드 → 변경 시에만 다운로드(304). network-first+ETag로 신선도 유지.
### 기능·법적
- v7 가짜 강사평점 제거 — 실제 상호(홈플러스·롯데마트·갤러리아 등)에 허구 강사 12명·가짜 별점을 결부(명예훼손·기만 소지)하던 것 삭제, "준비 중" 안내로 대체.
### 검증
- 프로덕션 빌드 0에러, node --check 전 JS 0오류, npm audit 0취약점, E2E 핵심3시나리오 7/7, 코드리뷰 교차검증.

## improve/20260609 — 상용화 개선 (진행 중)
### 보안
- 저장형 XSS 19곳 차단: esc() 헬퍼 추가(v4~v8) + 사용자입력(노트·위시리스트·커뮤니티·일기·알림·플래너·닉네임) 이스케이프.
- 하드코딩 비밀키 스캔: 0건 확인.

### 데이터·개인정보 (우회전략)
- 강사 실명 180,152건 제거(공개 배포본). 학부모 선택 필수정보(강좌명·가격·요일·시간·시작일·기간·카테고리)는 전부 유지.
- 내부 전체데이터는 .full.bak로 보존(gitignore, 공개 차단).

### 빌드·테스트·문서
- 프로덕션 빌드 도입(esbuild, JSX 사전컴파일 → Babel-인-브라우저 제거): dist/ 생성.
- 핵심 3시나리오 E2E 테스트(Playwright): tests/e2e_munsen.py.
- 배포·롤백 가이드(DEPLOY.md), 진행상황(PROGRESS.md), 개선계획서(개선계획서.html).

### 다음 (계획)
- 성능(29MB 데이터 분할 로드), 첫화면 정리(런처 통합), 접근성(WCAG AA), 끊긴 기능 복구, 시간표 기능.
